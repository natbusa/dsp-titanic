data:
    resources:
        train:
            path: clean/train.csv
            type: text
            format: csv

            options:
                header: true
                inferSchema: true

            provider: local
            flow: input

        test:
            sample : 1.0
            path : clean/test.csv
            type: text
            format: csv

            provider: local
            flow: input
